@model GamesIndexViewModel

@{
    ViewData["Title"] = "Games";
}

<h2>Games</h2>

<div class="card">
    @foreach (var game in Model.Games)
    {
        <p><a class="list-group-item" href="/games/@game.Id">@game.Title</a></p>
    }
</div>